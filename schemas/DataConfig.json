{
  "title": "DataConfig",
  "description": "Defines the data configuration for the NeuronBridge. ",
  "type": "object",
  "properties": {
    "prefixes": {
      "title": "Prefixes",
      "description": "Path prefixes for each file type in Files. If no prefix exists for a given file type, then the path should be treated as absolute.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "anatomicalRegions": {
      "title": "Anatomical regions",
      "description": "List of the anatomical regions that can be searched.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/AnatomicalRegion"
      }
    }
  },
  "required": [
    "prefixes",
    "anatomicalRegions"
  ],
  "additionalProperties": false,
  "definitions": {
    "AnatomicalRegion": {
      "title": "AnatomicalRegion",
      "description": "Defines an anatomical region of the fly brain that can be searched using NeuronBridge. All searches are specific to one region.",
      "type": "object",
      "properties": {
        "value": {
          "title": "Region name",
          "description": "Internal identifier for the anatomical region.",
          "type": "string"
        },
        "label": {
          "title": "Region label",
          "description": "Label used for the anatomical region in the UI.",
          "type": "string"
        },
        "alignmentSpace": {
          "title": "Alignment space",
          "description": "Alignment space to which this images in this region are registered.",
          "type": "string"
        },
        "disabled": {
          "title": "Disabled flag",
          "description": "True if this region is disabled in the UI.",
          "type": "boolean"
        }
      },
      "required": [
        "value",
        "label",
        "alignmentSpace"
      ],
      "additionalProperties": false
    }
  }
}
